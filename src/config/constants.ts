export const APP_NAME = "PrepRoute";
export const DEFAULT_PAGE_SIZE = 10;
export const DEBOUNCE_DELAY = 500;
export const REQUEST_TIMEOUT = 30000;
export const USERID = {
  MAX_LENGTH: 50,
};
export const PASSWORD = {
  MIN_LENGTH: 6,
  MAX_LENGTH: 18,
} as const;
